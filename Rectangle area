int length [5] = {4, 8, 12, 16, 20};
int width [5] = {2, 4, 6, 8, 10};

void setup()
{
Serial.begin (9600);
 
      
}
void loop()
{
 for (int l = 0; l < 5; l ++){
    for (int w = 0; w < 5; w ++){
      int area = length [l] * width [w];
Serial.println (area);
    }
 }
}
